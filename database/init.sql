-- Create the application database.
-- Run this as a PostgreSQL admin user.
CREATE DATABASE employee_db;

-- The Spring Boot application creates the employees table automatically
-- because spring.jpa.hibernate.ddl-auto=update.
